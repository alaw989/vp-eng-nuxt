---
phase: 17-accessibility-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - components/BackToTop.vue
  - components/ServiceCard.vue
  - components/ProjectCard.vue
  - components/TeamMember.vue
  - components/TestimonialCard.vue
  - components/SocialShare.vue
  - components/PwaReloadPrompt.vue
  - components/PwaInstallPrompt.vue
  - components/AppBreadcrumbs.vue
  - components/ClientLogos.vue
  - components/SearchResultSkeleton.vue
  - components/TeamMemberSkeleton.vue
  - components/ProjectDetailSkeleton.vue
  - components/ServiceDetailSkeleton.vue
  - components/LoadingSkeleton.vue
autonomous: true

must_haves:
  truths:
    - "All icon-only buttons have aria-label describing their action"
    - "All landmark regions have unique aria-label where multiple exist"
    - "Navigation links have descriptive aria-label where needed"
    - "Card components have proper ARIA attributes for screen readers"
    - "Decorative icons have aria-hidden='true'"
    - "Images have meaningful alt text (or empty alt for decorative)"
  artifacts:
    - path: "components/BackToTop.vue"
      provides: "Icon button with aria-label example"
      contains: "aria-label"
    - path: "components/ServiceCard.vue"
      provides: "Card with proper ARIA labeling"
      contains: "aria-label"
    - path: "components/ProjectCard.vue"
      provides: "Project card with accessible alt text"
      contains: ":alt"
  key_links:
    - from: "components/BackToTop.vue"
      to: "screen readers"
      via: "aria-label attribute"
      pattern: 'aria-label=.*Scroll to top'
    - from: "components/ServiceCard.vue"
      to: "screen readers"
      via: "NuxtLink aria-label"
      pattern: "aria-label="
    - from: "components/"
      to: "WCAG A11Y-02"
      via: "ARIA label attributes"
      pattern: 'aria-label=|aria-labelledby='
---

<objective>
Add comprehensive ARIA labels and landmarks to all interactive components throughout the application. This ensures screen reader users can understand and interact with all UI elements, satisfying requirement A11Y-02.

Purpose: A11Y-02 requires all interactive elements to have ARIA labels. Icon-only buttons, decorative elements, card components, and navigation elements need proper labeling for screen reader accessibility.

Output: All components have appropriate ARIA labels, landmarks are properly defined, and icon buttons are screen reader accessible.
</objective>

<execution_context>
@/home/deck/.claude/get-shit-done/workflows/execute-plan.md
@/home/deck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/17-accessibility-foundation/17-RESEARCH.md

# Research findings:
- "No ARIA is better than bad ARIA" - use native HTML semantics first
- Use aria-label for icon-only buttons
- Use aria-labelledby to reference visible text
- Don't add role="navigation" to <nav> (implicit)
- Don't add role="button" to <button> (implicit)
- Use aria-hidden="true" on decorative icons

# Current state from codebase review:
- BackToTop.vue: HAS aria-label (good!) - line 14
- AppHeader.vue: HAS aria-label on nav, aria-current on active links (good!)
- HeroSlider.vue: HAS aria-label, aria-live (good!)
- ProjectGallery.vue: HAS role="dialog", aria-modal, aria-label (good!)

# Components needing ARIA review:
- ServiceCard.vue: Icon in div, needs aria-hidden on icon or label on container
- ProjectCard.vue: Uses meaningful alt text (good!), may need aria-label on link
- TeamMember.vue: Likely needs review for image alt, card labeling
- TestimonialCard.vue: Check for proper semantic structure
- SocialShare.vue: Icon buttons need aria-label
- PwaReloadPrompt.vue: Icon button needs aria-label
- PwaInstallPrompt.vue: Icon button needs aria-label
- AppBreadcrumbs.vue: aria-label on nav, current page indication
- ClientLogos.vue: Decorative images need empty alt or aria-hidden
- Skeleton components: aria-hidden="true" (loading placeholders)
</context>

<tasks>

<task type="auto">
  <name>Add ARIA labels to icon buttons and interactive components</name>
  <files>
    components/BackToTop.vue
    components/SocialShare.vue
    components/PwaReloadPrompt.vue
    components/PwaInstallPrompt.vue
    components/AppBreadcrumbs.vue
  </files>
  <action>
    Review and enhance ARIA labels on icon-based components:

    1. **components/BackToTop.vue** - Already has aria-label (line 14), verify it's descriptive enough

    2. **components/SocialShare.vue** - Add aria-label to each social platform button:
       ```vue
       <button
         :aria-label="`Share on ${platform}`"
         @click="share"
       >
         <Icon :name="icon" />
       </button>
       ```

    3. **components/PwaReloadPrompt.vue** - Ensure reload button has aria-label="Reload app for updates"

    4. **components/PwaInstallPrompt.vue** - Ensure install button has aria-label="Install app"

    5. **components/AppBreadcrumbs.vue** - Add aria-label to nav:
       ```vue
       <nav aria-label="Breadcrumb">
       ```
       Ensure current page has aria-current="page"

    DO NOT add role="button" to <button> elements (redundant).
    DO NOT add role="navigation" to <nav> elements (redundant).
  </action>
  <verify>
    1. `grep -r "aria-label" components/SocialShare.vue components/PwaReloadPrompt.vue components/PwaInstallPrompt.vue components/AppBreadcrumbs.vue` shows aria-label attributes
    2. `npm run build` completes without errors
    3. No TypeScript errors related to missing aria-label
  </verify>
  <done>
    All icon buttons have descriptive aria-label attributes for screen reader users
  </done>
</task>

<task type="auto">
  <name>Add ARIA labels to card components</name>
  <files>
    components/ServiceCard.vue
    components/ProjectCard.vue
    components/TeamMember.vue
    components/TestimonialCard.vue
  </files>
  <action>
    Enhance card components with proper ARIA labeling:

    1. **components/ServiceCard.vue** - Add aria-label to the NuxtLink:
       ```vue
       <NuxtLink
         :to="`/services/${slug}`"
         :aria-label="`Learn more about ${title} services`"
       >
       ```
       Add aria-hidden="true" to the decorative Icon:
       ```vue
       <Icon :name="icon" aria-hidden="true" />
       ```

    2. **components/ProjectCard.vue** - Verify alt text is descriptive (currently good based on review). Add aria-label if needed:
       ```vue
       <NuxtLink
         :aria-label="`View project: ${title}${location ? ` in ${location}` : ''}`"
       >
       ```

    3. **components/TeamMember.vue** - Add aria-label and proper alt:
       ```vue
       <NuxtLink
         :aria-label="`View ${name}'s profile`"
       >
       <img :alt="`Portrait of ${name}, ${title}`" />
       ```

    4. **components/TestimonialCard.vue** - Add blockquote structure and aria-label:
       ```vue
       <figure aria-label="Testimonial from ${author}">
         <blockquote>{{ text }}</blockquote>
         <figcaption>{{ author }}</figcaption>
       </figure>
       ```

    DO NOT wrap cards in unnecessary divs with ARIA roles.
    Use native HTML elements (figure, blockquote, figcaption) where appropriate.
  </action>
  <verify>
    1. `grep "aria-label" components/ServiceCard.vue components/ProjectCard.vue components/TeamMember.vue components/TestimonialCard.vue` shows labels
    2. `grep "aria-hidden" components/ServiceCard.vue` shows decorative icon is hidden
    3. `npm run build` completes without errors
  </verify>
  <done>
    All card components have descriptive ARIA labels and proper semantic HTML structure
  </done>
</task>

<task type="auto">
  <name>Add ARIA attributes to utility and skeleton components</name>
  <files>
    components/ClientLogos.vue
    components/SearchResultSkeleton.vue
    components/TeamMemberSkeleton.vue
    components/ProjectDetailSkeleton.vue
    components/ServiceDetailSkeleton.vue
    components/LoadingSkeleton.vue
  </files>
  <action>
    Add appropriate ARIA attributes to utility components:

    1. **components/ClientLogos.vue** - Mark decorative images:
       ```vue
       <img aria-hidden="true" ... />
       ```
       or use empty alt if image is decorative:
       ```vue
       <img alt="" ... />
       ```
       Add aria-label to container if it represents "Our clients":
       ```vue
       <div aria-label="Our clients" role="complementary">
       ```

    2. **All Skeleton components** - Add aria-hidden to hide loading placeholders:
       ```vue
       <div aria-hidden="true" class="skeleton">
       ```
       This prevents screen readers from announcing "loading" placeholders.

    3. **components/LoadingSkeleton.vue** - Add aria-hidden="true" and role="status":
       ```vue
       <div role="status" aria-live="polite" aria-label="Loading">
         <div aria-hidden="true" class="skeleton-animation">
       ```

    DO NOT add aria-live to skeleton components (they're decorative).
    DO use role="status" on the loading container with aria-label.
  </action>
  <verify>
    1. `grep "aria-hidden" components/ClientLogos.vue components/*Skeleton.vue components/LoadingSkeleton.vue` shows attributes
    2. `grep "role=\"status\"" components/LoadingSkeleton.vue` shows status role
    3. `npm run build` completes without errors
  </verify>
  <done>
    Utility components have proper ARIA attributes (decorative elements hidden, loading content properly labeled)
  </done>
</task>

</tasks>

<verification>
After all tasks complete, verify:
1. `npm run dev` starts without errors
2. DevTools A11y panel shows no missing ARIA label violations
3. All icon buttons have aria-label (checked via grep)
4. All decorative elements have aria-hidden="true" or empty alt
5. All cards have descriptive aria-label
6. Navigate with screen reader simulation (VoiceOver/TalkBack) to verify announcements
</verification>

<success_criteria>
1. All icon-only buttons have descriptive aria-label
2. All card components have aria-label describing link destination
3. Decorative icons and images have aria-hidden="true"
4. Loading skeletons have aria-hidden="true"
5. No "Elements must have accessible name" violations in axe-core
6. Navigation has aria-label for landmark identification
</success_criteria>

<output>
After completion, create `.planning/phases/17-accessibility-foundation/17-02-SUMMARY.md` with:
- List of components enhanced with ARIA labels
- Any components that couldn't be enhanced (and why)
- axe-core scan results before and after
- Notes on aria-hidden usage for decorative elements
</output>
