---
phase: 10-section-polish---contact---careers
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [pages/contact.vue]
autonomous: true

must_haves:
  truths:
    - "Contact form displays with proper input styling and visual feedback"
    - "Form validation shows clear error messages with accessible ARIA attributes"
    - "Submit button provides loading state with spinner icon during submission"
    - "Success/error states display with appropriate colors and icons"
    - "Honeypot field remains hidden and functional for spam protection"
  artifacts:
    - path: "pages/contact.vue"
      provides: "Contact page with polished form styling"
      min_lines: 500
  key_links:
    - from: "pages/contact.vue"
      to: "server/api/contact.post.ts"
      via: "$fetch POST request on form submit"
      pattern: "\\$fetch.*api/contact.*method.*POST"
---

<objective>
Polish contact form styling and validation feedback for visual consistency and accessibility. The contact page already has solid implementation - this plan adds hover effects, focus states, and refined visual polish matching Phases 6-9 patterns.

Purpose: Ensure contact form provides excellent UX with clear visual feedback and accessible error handling
Output: Refined contact form with polished styling, enhanced focus states, and consistent visual feedback
</objective>

<execution_context>
@/home/deck/.claude/get-shit-done/workflows/execute-plan.md
@/home/deck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@pages/contact.vue
@server/api/contact.post.ts
@components/AppSection.vue
@.planning/phases/06-homepage-polish/06-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Polish contact form input styling with hover and focus effects</name>
  <files>pages/contact.vue</files>
  <action>
    Update pages/contact.vue form input styling:

    1. Add hover effects on form inputs:
       - Add hover:border-primary/50 to all input, select, and textarea elements
       - This provides subtle visual feedback when user hovers over form fields

    2. Enhance focus ring styling:
       - Update focus:ring-2 to focus:ring-4 for more prominent focus indicator
       - Add focus:ring-offset-2 to create visual separation from content
       - Ensure focus states work for keyboard navigation (Tab key)

    3. Add smooth transitions to form elements:
       - Add transition-all duration-200 to input, select, textarea
       - Add transition-colors duration-200 to submit button
       - This matches the transition patterns from Phase 6-9

    4. Polish form field labels:
       - Ensure labels have consistent text-sm font-semibold styling
       - Keep required field asterisks with text-red-500 and aria-hidden="true"

    DO NOT change:
    - Form validation logic (validateForm, validateEmail functions)
    - API endpoint (/api/contact)
    - Honeypot field implementation
    - Form field structure or data model
  </action>
  <verify>grep -E "hover:border-primary|transition-all|duration-200|focus:ring-4" /home/deck/Sites/vp-eng-nuxt/pages/contact.vue</verify>
  <done>Form inputs show hover border effect, focus ring is prominent (ring-4), and all transitions use duration-200 for smooth UX</done>
</task>

<task type="auto">
  <name>Enhance form submission states and error message styling</name>
  <files>pages/contact.vue</files>
  <action>
    Update pages/contact.vue submission feedback styling:

    1. Polish submit button loading state:
       - Current isSubmitting state with spinner is good - keep as-is
       - Ensure disabled:opacity-50 and disabled:cursor-not-allowed are present
       - Add hover:bg-primary-dark transition for non-disabled state

    2. Enhance success/error message styling:
       - Update success state: Use bg-green-50 (lighter background) with border-green-200 and text-green-800
       - Update error state: Use bg-red-50 with border-red-200 and text-red-800
       - Add shadow-sm to message container for depth
       - Add rounded-lg for consistent corner radius

    3. Add animation to submit messages:
       - Add animate-fade-in class to message div
       - Define fade-in animation in component style block (0.3s ease-in)
       - This provides smooth appearance when messages appear

    4. Polish form action buttons:
       - Ensure "Send Message" button has proper hover state
       - Keep focus-visible:ring for keyboard accessibility

    DO NOT change:
    - Form submission logic (handleSubmit function)
    - Error handling logic
    - API integration
  </action>
  <verify>grep -E "bg-green-50|bg-red-50|animate-fade|shadow-sm" /home/deck/Sites/vp-eng-nuxt/pages/contact.vue</verify>
  <done>Success messages use green-50 background, error messages use red-50 background, messages fade in smoothly, and button states are visually clear</done>
</task>

<task type="auto">
  <name>Add subtle hover effects to contact information cards</name>
  <files>pages/contact.vue</files>
  <action>
    Update pages/contact.vue contact information section styling:

    1. Add hover effects to contact info cards:
       - Each contact item (address, phone, email, hours) is in a div with flex items-start gap-4
       - Add group class to parent div
       - Add group-hover:translate-x-1 to icon container for subtle movement
       - Add transition-all duration-300 for smooth animation

    2. Enhance link hover states:
       - Phone and email links should have hover:text-primary-dark
       - Add hover:underline for additional feedback
       - Keep transition-colors duration-200

    3. Polish icon containers:
       - Current w-12 h-12 bg-primary/10 rounded-lg is good
       - Add group-hover:bg-primary/20 for subtle background change on hover
       - Add transition-colors duration-200 to icon container

    4. Emergency contact card:
       - Current red-50 bg with border-red-200 is good
       - Add hover:shadow-md transition for depth on hover
       - Keep existing icon and styling

    DO NOT change:
    - Contact information content
    - Icon names (mdi:map-marker, mdi:phone, mdi:email, mdi:clock, mdi:alert-circle)
    - Phone number or email addresses
    - Map embed code
  </action>
  <verify>grep -E "group-hover:translate-x-1|group-hover:bg-primary/20|hover:shadow-md" /home/deck/Sites/vp-eng-nuxt/pages/contact.vue</verify>
  <done>Contact info items have subtle hover movement (translate-x-1), icon backgrounds lighten on hover, and emergency card has hover shadow effect</done>
</task>

</tasks>

<verification>
1. Build completes without errors: npm run build
2. Contact page loads at /contact with proper form styling
3. Form inputs show hover and focus states clearly
4. Submit button shows loading state during submission
5. Success/error messages display with appropriate colors and fade-in animation
6. Contact information cards have hover effects
7. All transitions use duration-200 or duration-300 matching Phase 6-9 patterns
</verification>

<success_criteria>
1. Contact form displays with polished styling matching other section polish phases
2. Form validation provides clear visual feedback with accessible error messages
3. Submit states (loading, success, error) are visually distinct and animated
4. Contact information cards have hover effects for interactivity
5. Build passes with no hydration errors or TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/10-section-polish---contact---careers/10-01-SUMMARY.md` with:
- Duration and completion timestamp
- Files modified (pages/contact.vue)
- Styling changes made (hover effects, focus states, transitions)
- Verification results
- Any deviations from plan
</output>
