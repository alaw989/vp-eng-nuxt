---
phase: 22-hero-modernization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [components/HeroSlider.vue, components/HeroStatic.vue, pages/index.vue]
autonomous: false

must_haves:
  truths:
    - "New static hero component renders without carousel functionality"
    - "Hero displays single value proposition headline (bold, 5-7 words)"
    - "Hero background image displays with proper optimization (WebP, eager load)"
    - "Navigation arrows and dot indicators are removed (no carousel controls)"
    - "Mobile responsive layout works (vertical stacking, proper text sizing)"
  artifacts:
    - path: "components/HeroStatic.vue"
      provides: "Static hero component with modern design"
      min_lines: 150
    - path: "components/HeroSlider.vue"
      provides: "Archived carousel component (kept for reference/rollback)"
      contains: "carousel functionality"
    - path: "pages/index.vue"
      provides: "Homepage using static hero instead of slider"
      exports: ["HeroStatic"]
  key_links:
    - from: "components/HeroStatic.vue"
      to: "public/images/hero/"
      via: "NuxtImg with eager loading"
      pattern: "NuxtImg.*loading.*eager"
    - from: "pages/index.vue"
      to: "components/HeroStatic.vue"
      via: "component import"
      pattern: "HeroStatic"
    - from: "components/HeroStatic.vue"
      to: "composables/useA11y.ts"
      via: "useAnnouncer for screen reader (if needed)"
      pattern: "useAnnouncer"
---

<objective>
Create a modern static hero component to replace the 9-slide carousel, based on competitive research findings that static heroes improve conversions and user engagement.

Purpose: Modernize the hero section to align with 2026 engineering industry standards (5/7 top firms use static heroes) and improve UX by eliminating carousel fatigue (84% of carousel clicks happen on slide 1).

Output: A new HeroStatic component with bold typography, single value proposition headline, and optimized imagery; HeroSlider archived for reference; homepage updated to use static hero.
</objective>

<execution_context>
@/Users/austinlaw/.claude/get-shit-done/workflows/execute-plan.md
@/Users/austinlaw/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-hero-modernization/22-RESEARCH.md
@components/HeroSlider.vue
@pages/index.vue
@.planning/phases/17-accessibility-foundation/17-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create HeroStatic component with modern design</name>
  <files>components/HeroStatic.vue</files>
  <action>
Create a new static hero component with the following implementation:

1. Component structure:
   - Full-screen hero (h-[80vh] min-h-[600px]) matching current dimensions
   - Single background image using NuxtImg with loading="eager", fetchpriority="high"
   - Bold headline (5-7 words max) using text-5xl md:text-7xl, font-bold
   - Optional subheadline (2 lines max) for supporting context
   - Single CTA button or no CTA (research shows many leading firms omit hero CTAs)

2. Visual design (per research):
   - Bold typography as primary visual element ("big type" trend)
   - Background: High-quality project image with gradient overlay (from-primary/80 to-black/80)
   - Optional subtle parallax using VueUse useWindowScroll (respect prefers-reduced-motion)
   - Clean, modern layout with centered content

3. Default content (using research-recommended headline):
   - Headline: "Trusted by Tampa Bay Since 1990" (authority-focused from research options)
   - Subheadline: "Comprehensive structural engineering services standing the test of time"
   - CTA: "Let's Talk" linking to /contact (conversational per research)
   - Background: /images/hero/toronto-marina-1920w.jpg (waterfront shows Tampa Bay context)

4. Component props interface:
   ```typescript
   interface HeroProps {
     headline?: string
     subheadline?: string
     ctaText?: string
     ctaLink?: string
     backgroundImage?: string
     backgroundAlt?: string
     showCta?: boolean
   }
   ```

5. Accessibility:
   - Proper heading hierarchy (h1 for headline)
   - aria-label on section if needed
   - All images have alt text
   - Respect prefers-reduced-motion for parallax

Do NOT include:
- Navigation arrows (left/right buttons)
- Dot indicators
- Progress bar
- Autoplay functionality
- Touch swipe handlers
- Keyboard navigation for slides (no slides to navigate)
  </action>
  <verify>Component file exists at components/HeroStatic.vue with ~150+ lines, renders without console errors</verify>
  <done>HeroStatic component created with modern design, bold typography, and single value proposition headline</done>
</task>

<task type="auto">
  <name>Task 2: Archive HeroSlider and update homepage</name>
  <files>components/HeroSlider.vue, pages/index.vue</files>
  <action>
1. Archive HeroSlider for reference/rollback:
   - Rename HeroSlider.vue to HeroSlider.vue.archive (keeps functionality intact if rollback needed)
   - Add comment at top: <!-- ARCHIVED 2026-02-07: Replaced by HeroStatic per competitive research -->

2. Update pages/index.vue:
   - Replace `<HeroSlider />` with `<HeroStatic />`
   - Remove any hero-specific props being passed to HeroSlider
   - Keep all other homepage sections unchanged (About Intro, Statistics, Services, etc.)

3. Test render:
   - Homepage should load with static hero
   - No carousel functionality visible
   - No navigation arrows or dots
   - Single headline displays correctly
  </action>
  <verify>
1. HeroSlider.vue.archive exists
2. pages/index.vue imports HeroStatic instead of HeroSlider
3. Dev server runs without errors
  </verify>
  <done>HeroSlider archived, homepage updated to use HeroStatic component</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete static hero implementation replacing the 9-slide carousel</what-built>
  <how-to-verify>
1. Start dev server: npm run dev
2. Visit http://localhost:3000
3. Verify the following:
   - Hero displays with single headline (not rotating)
   - No navigation arrows visible
   - No dot indicators at bottom
   - No progress bar
   - Text is bold and prominent (big type design)
   - Background image loads quickly
   - Single CTA button (or no CTA if design choice)
   - Mobile responsive: text stacks properly, still readable

4. Test accessibility:
   - Tab through: focus should go to navigation, then CTA (no slider controls)
   - Screen reader: announces h1 headline correctly
   - No auto-annoying motion (parallax respects prefers-reduced-motion)

5. Compare to research examples (optional):
   - Review Thornton Tomasetti, Arup, DCI Engineers sites for comparison
   - Verify similar "static, single message" approach

6. Check performance:
   - Lighthouse LCP should be < 2.5s (single image loads faster than carousel)
   - No layout shift (hero has fixed dimensions)
  </how-to-verify>
  <resume-signal>Type "approved" if the static hero looks modern and professional, or describe any issues with layout, typography, or imagery</resume-signal>
</task>

</tasks>

<verification>
After task completion and user approval, verify:
- [ ] HeroStatic.vue exists and renders without errors
- [ ] HeroSlider.vue.archive exists (rollback capability preserved)
- [ ] pages/index.vue uses HeroStatic component
- [ ] No carousel functionality (arrows, dots, progress bar removed)
- [ ] Hero has bold headline (5-7 words recommended)
- [ ] Mobile responsive layout works
- [ ] Accessibility maintained (h1, alt text, respects prefers-reduced-motion)
- [ ] Performance: LCP < 2.5s, no CLS
</verification>

<success_criteria>
1. Static hero component replaces carousel with modern design
2. Bold typography headline communicates value proposition clearly
3. No carousel controls (arrows, dots, progress bar)
4. Mobile responsive with proper text sizing
5. Accessibility preserved (WCAG 2.1 AA compliant)
6. Performance improved over carousel (faster LCP, no multiple image loads)
</success_criteria>

<output>
After completion, create `.planning/phases/22-hero-modernization/22-01-SUMMARY.md`
</output>
