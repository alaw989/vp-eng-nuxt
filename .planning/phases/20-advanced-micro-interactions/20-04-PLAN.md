---
phase: 20-advanced-micro-interactions
plan: 04
type: execute
wave: 2
depends_on: ["20-01"]
files_modified:
  - components/TestimonialsSlider.vue
autonomous: false

must_haves:
  truths:
    - "Testimonial carousel slides transition in 300ms (reduced from 500ms)"
    - "Testimonial cards fade in when slide changes"
    - "Reduced-motion users see instant slide change (no sliding animation)"
    - "Carousel announces slide changes to screen readers (aria-live)"
  artifacts:
    - path: "components/TestimonialsSlider.vue"
      provides: "Polished testimonial carousel with 300ms transitions"
      contains: "usePreferredReducedMotion, TransitionGroup, card-fade classes"
  key_links:
    - from: "components/TestimonialsSlider.vue"
      to: "@vueuse/core"
      via: "usePreferredReducedMotion import"
      pattern: "usePreferredReducedMotion.*from.*@vueuse/core"
    - from: "components/TestimonialsSlider.vue"
      to: "components/TestimonialCard.vue"
      via: "TransitionGroup wrapping cards"
      pattern: "TransitionGroup.*name.*card-fade"
---

<objective>
Polish testimonial carousel transitions with 300ms timing, card fade-in animations, and reduced-motion support.

Purpose: Consistent micro-interaction timing (300ms standard), engaging card animations, accessible for motion-sensitive users
Output: Enhanced TestimonialsSlider with reduced-motion check, TransitionGroup for cards, 300ms slide transitions
</objective>

<execution_context>
@/Users/austinlaw/.claude/get-shit-done/workflows/execute-plan.md
@/Users/austinlaw/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/20-advanced-micro-interactions/20-RESEARCH.md
@.planning/phases/18-core-micro-interactions/18-01-SUMMARY.md
@.planning/phases/19-page-transitions/19-01-SUMMARY.md
@.planning/phases/20-advanced-micro-interactions/20-01-SUMMARY.md

@components/TestimonialsSlider.vue
@components/TestimonialCard.vue
@composables/usePageTransition.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add reduced-motion support and 300ms timing to TestimonialsSlider</name>
  <files>components/TestimonialsSlider.vue</files>
  <action>
    Update components/TestimonialsSlider.vue for polished transitions:

    1. Import usePreferredReducedMotion and TransitionGroup:
       Add:
       import { usePreferredReducedMotion } from '@vueuse/core'
       import { TransitionGroup } from 'vue'

    2. Initialize reduced motion detection in script setup:
       const { prefersReducedMotion } = usePreferredReducedMotion()

    3. Update slide track div (line 9-13):
       - Change class from "flex transition-transform duration-500 ease-out"
       - To dynamic class: :class="prefersReducedMotion ? '' : 'transition-transform duration-300 ease-out'"
       - Update style binding: :style="!prefersReducedMotion ? { transform: `translateX(-${currentIndex * 100}%)` } : {}"

    4. Wrap TestimonialCard grid with TransitionGroup:
       Find: <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
       Change to:
       <TransitionGroup name="card-fade" tag="div" class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">

    5. Add CSS for card fade-in animation in <style scoped>:
       .card-fade-enter-active {
         transition: opacity 300ms ease-out, transform 300ms ease-out;
       }
       .card-fade-enter-from {
         opacity: 0;
         transform: translateY(20px);
       }

    6. Add reduced-motion CSS:
       @media (prefers-reduced-motion: reduce) {
         .card-fade-enter-active {
           transition: opacity 150ms linear;
         }
         .card-fade-enter-from {
           transform: none;
         }
       }

    Per RESEARCH.md lines 827-973 (enhanced TestimonialsSlider pattern)

    DO NOT change keyboard navigation (arrow keys, Home, End).
    DO NOT remove aria-live="polite" screen reader announcements.
    DO NOT modify navigation arrows or dot indicators.
  </action>
  <verify>
    1. Check components/TestimonialsSlider.vue imports usePreferredReducedMotion
    2. Check slide track uses duration-300 (not duration-500)
    3. Check TransitionGroup wraps TestimonialCard grid
    4. Check CSS includes .card-fade-enter-active and .card-fade-enter-from
    5. Check @media (prefers-reduced-motion: reduce) block exists
  </verify>
  <done>
    TestimonialsSlider uses 300ms transitions, cards fade in on slide change, reduced-motion users see instant changes
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Polished testimonial carousel with 300ms transitions and card fade-in animations</what-built>
  <how-to-verify>
    1. Run npm run dev
    2. Navigate to homepage (or page with TestimonialsSlider)
    3. Test carousel transitions:
       - Click next/prev arrows
       - Click dot indicators
       - Use arrow keys (keyboard navigation)
    4. Verify slide transition takes 300ms (snappy, not sluggish)
    5. Verify cards fade in with subtle translateY when slide changes
    6. Enable OS reduced motion (macOS: System Settings > Accessibility > Display > Reduce motion)
    7. Verify slide change is instant (no sliding, no fade animation)
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues with carousel transitions</resume-signal>
</checkpoint>
</task>

</tasks>

<verification>
1. Slide transitions complete in 300ms (down from 500ms)
2. Testimonial cards fade in with translateY(20px) when slide changes
3. Reduced-motion users see instant slide change (no sliding animation)
4. Keyboard navigation (arrow keys, Home, End) still works
5. Screen reader announcements work (aria-live="polite" region)
6. Navigation arrows and dot indicators function correctly
</verification>

<success_criteria>
- TestimonialsSlider imports and uses usePreferredReducedMotion
- Slide transitions use duration-300 (aligns with Phase 18 300ms standard)
- TransitionGroup wraps TestimonialCard grid with card-fade name
- CSS includes .card-fade-enter-active with 300ms ease-out
- Reduced-motion CSS removes transforms, keeps 150ms linear opacity
- Keyboard navigation and screen reader support preserved
</success_criteria>

<output>
After completion, create `.planning/phases/20-advanced-micro-interactions/20-04-SUMMARY.md`
</output>
