---
phase: 18-core-micro-interactions
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pages/index.vue
  - pages/about.vue
  - pages/contact.vue
  - pages/careers/index.vue
  - pages/projects/index.vue
  - pages/services/index.vue
  - components/AppHeader.vue
  - components/AppFooter.vue
autonomous: true

must_haves:
  truths:
    - "All buttons show hover state with color change and subtle transform (300ms per Phase 22 standard, exceeds MICRO-01's 150-200ms requirement)"
    - "All navigation links show hover indication with color shift"
    - "Inline text links show hover indication (underline or color shift)"
    - "All hover states use duration-300 for consistency (per Phase 22 standard)"
    - "Reduced motion users see color feedback without transforms"
  artifacts:
    - path: "pages/index.vue"
      provides: "Homepage button hover states"
      contains: "hover:-translate-y-0.5"
    - path: "components/AppHeader.vue"
      provides: "Navigation link hover states"
      contains: "hover:text-primary"
    - path: "components/AppFooter.vue"
      provides: "Footer link hover states"
      contains: "hover:text-white"
  key_links:
    - from: "All button elements"
      to: "Hover state classes"
      via: "Tailwind hover:-translate-y-0.5 utility"
      pattern: "hover:-translate-y-0\\.5"
    - from: "All link elements"
      to: "Hover state classes"
      via: "Tailwind hover:text-* utility"
      pattern: "hover:text-(primary|white)"
---

<objective>
Standardize button and link hover states across all pages and components with consistent 300ms timing, subtle transforms, and reduced-motion support. This implements MICRO-01 (button hover states) and MICRO-02 (link hover states) requirements.

Purpose: Responsive visual feedback on all interactive elements improves perceived responsiveness and user experience. Phase 22 established 300ms as the standard micro-interaction duration, which must be applied consistently across the site.

Output: All buttons and links have standardized hover states with color shifts, subtle transforms, and consistent timing.
</objective>

<execution_context>
@/Users/austinlaw/.claude/get-shit-done/workflows/execute-plan.md
@/Users/austinlaw/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/18-core-micro-interactions/18-core-micro-interactions-RESEARCH.md
@.planning/phases/22-hero-modernization/22-hero-modernization-VERIFICATION.md

# Phase 22 established patterns:
- 300ms micro-interaction duration (standard timing)
- Group hover pattern for coordinated parent-child states
- Reduced-motion support preserves color feedback while disabling transforms
- Button hover: lift effect (-translate-y-0.5) with shadow increase

# Current state from codebase audit:
- 20+ button implementations across 17 pages
- Buttons have color shifts but missing subtle transform (-translate-y-0.5)
- Inconsistent timing: some use 200ms, some use 300ms
- Navigation links have color shift (hover:text-primary)
- Footer links have color shift (hover:text-white)
- Inline links inconsistent (some have underline, some don't)
</context>

<tasks>

<task type="auto">
  <name>Update all button hover states with consistent timing and transforms</name>
  <files>pages/index.vue, pages/about.vue, pages/contact.vue, pages/careers/index.vue, pages/projects/index.vue, pages/services/index.vue</files>
  <action>
    For each page with buttons, update button classes to include:

    1. Add `hover:-translate-y-0.5` for subtle lift effect
    2. Ensure `transition-all duration-300` for consistent timing (replace any duration-200)
    3. Ensure `hover:bg-primary-dark` or equivalent color shift exists
    4. Keep existing `focus-visible:ring-2 focus-visible:ring-primary focus-visible:ring-offset-2` classes

    Standard button pattern:
    ```vue
    <button class="px-6 py-3 bg-primary text-white rounded-lg font-semibold
                  hover:bg-primary-dark hover:-translate-y-0.5
                  transition-all duration-300
                  focus-visible:ring-2 focus-visible:ring-primary focus-visible:ring-offset-2">
      Button Text
    </button>
    ```

    Pages to update (grep for button patterns):
    - pages/index.vue: Hero CTA button (line ~21)
    - pages/about.vue: Any buttons
    - pages/contact.vue: Submit button (line ~175)
    - pages/careers/index.vue: Apply buttons (line ~123)
    - pages/projects/index.vue: Any filter/sort buttons
    - pages/services/index.vue: Any buttons

    DO NOT create new components - update existing buttons in place.
  </action>
  <verify>
    1. `grep -r "hover:-translate-y-0.5" pages/` returns button matches across all pages
    2. `grep -r "duration-300" pages/ | grep -v "duration-3000" | grep button` shows consistent timing
    3. `npm run dev` starts without errors
    4. Visual test: Hover over buttons, verify subtle lift effect
  </verify>
  <done>
    All buttons have hover state with color shift, subtle transform (-translate-y-0.5), and consistent 300ms timing
  </done>
</task>

<task type="auto">
  <name>Verify and enhance navigation and footer link hover states</name>
  <files>components/AppHeader.vue, components/AppFooter.vue</files>
  <action>
    1. Review AppHeader.vue navigation links (lines ~24-25)
       - Verify `hover:text-primary` exists for color shift
       - Verify `transition-colors` or `transition-all duration-300` exists
       - Add `duration-300` if missing for consistency

    2. Review AppFooter.vue links
       - Verify `hover:text-white` or equivalent exists for footer links on dark background
       - Verify `transition-colors` or `transition-all duration-300` exists
       - Add `duration-300` if missing for consistency

    3. Ensure all links have focus-visible classes:
       - `focus-visible:ring-2 focus-visible:ring-primary focus-visible:ring-offset-2`
       - Add `rounded-lg px-2 py-1` for adequate hit target on focus

    DO NOT add underline animations to navigation links (color shift is standard pattern).
  </action>
  <verify>
    1. `grep "hover:text-primary" components/AppHeader.vue` returns navigation link matches
    2. `grep "hover:text-white" components/AppFooter.vue` returns footer link matches
    3. `grep "duration-300" components/AppHeader.vue components/AppFooter.vue` shows consistent timing
    4. Visual test: Hover over nav/footer links, verify color shift
  </verify>
  <done>
    Navigation and footer links show hover indication with color shift and consistent 300ms timing
  </done>
</task>

<task type="auto">
  <name>Add hover states to inline text links within page content</name>
  <files>pages/index.vue, pages/about.vue, pages/contact.vue, pages/careers/index.vue, pages/projects/index.vue, pages/services/index.vue</files>
  <action>
    For each page with inline text links, add hover indication:

    1. Search for inline anchor tags within content sections (not navigation/footer)
    2. Add hover classes to inline links:
       - `hover:text-primary` for color shift on light backgrounds
       - `hover:underline` for underline indication
       - `transition-colors duration-300` for consistent timing
       - `focus-visible:ring-2 focus-visible:ring-primary focus-visible:ring-offset-2 rounded px-1` for accessibility

    Standard inline link pattern:
    ```vue
    <a href="/path" class="text-primary hover:text-primary-dark hover:underline
                         transition-colors duration-300
                         focus-visible:ring-2 focus-visible:ring-primary focus-visible:ring-offset-2 rounded px-1">
      Link text
    </a>
    ```

    Pages to update (grep for inline anchor patterns):
    - pages/index.vue: Any inline links in hero or content sections
    - pages/about.vue: Any inline links in about content
    - pages/contact.vue: Any inline links (e.g., email address)
    - pages/careers/index.vue: Any inline links to job postings
    - pages/projects/index.vue: Any inline links
    - pages/services/index.vue: Any inline links

    DO NOT modify navigation or footer links (covered in Task 2).
    DO NOT add hover effects to links that already have them.
  </action>
  <verify>
    1. `grep -r "hover:underline" pages/ | grep -v "nav\|footer"` returns inline link matches
    2. `grep -r "hover:text-primary" pages/ | grep -v "nav\|footer\|button"` returns inline link color shifts
    3. `grep -r "transition-colors duration-300" pages/` shows consistent timing on links
    4. Visual test: Hover over inline links, verify underline + color shift
  </verify>
  <done>
    Inline text links within page content show hover indication (underline or color shift) with consistent 300ms timing
  </done>
</task>

<task type="auto">
  <name>Add global reduced motion support for micro-interactions</name>
  <files>assets/css/main.css</files>
  <action>
    Add global reduced motion support to assets/css/main.css:

    1. Add after the existing focus-visible styles (after line 100):

    ```css
    /* Reduced motion support - preserve color feedback, disable transforms */
    @media (prefers-reduced-motion: reduce) {
      *,
      *::before,
      *::after {
        animation-duration: 0.01ms !important;
        animation-iteration-count: 1 !important;
        transition-duration: 0.01ms !important;
        scroll-behavior: auto !important;
      }

      /* Disable transforms but preserve color feedback */
      .hover\\:-translate-y-0\\.5:hover,
      .hover\\:-translate-y-1:hover,
      .group-hover\\:-translate-y-1:hover,
      .group-hover\\:scale-105:hover,
      .group-hover\\:scale-110:hover {
        transform: none !important;
      }

      /* Keep color transitions for reduced motion users */
      .hover\\:bg-primary-dark:hover,
      .hover\\:text-primary:hover,
      .hover\\:text-white:hover {
        transition: background-color 300ms ease, color 300ms ease !important;
      }
    }
    ```

    2. Add comment before the section: "Standard micro-interaction duration: 300ms (per Phase 22)"

    DO NOT modify existing focus-visible styles or print styles.
  </action>
  <verify>
    1. `grep -A5 "@media (prefers-reduced-motion: reduce)" assets/css/main.css` shows the new section
    2. `grep "Standard micro-interaction duration" assets/css/main.css` shows the documentation comment
    3. `npm run dev` starts without CSS errors
    4. Test with macOS Reduce Motion: System Preferences > Accessibility > Display > Reduce motion, verify transforms are disabled
  </verify>
  <done>
    Global reduced motion support exists in main.css, preserving color feedback while disabling transforms
  </done>
</task>

</tasks>

<verification>
After all tasks complete, verify:
1. `npm run dev` starts without errors
2. Hover over any button: see color shift + subtle lift effect
3. Hover over nav/footer links: see color shift
4. Tab to any button/link: see focus ring
5. Test with reduced motion enabled: color changes persist, transforms disabled
6. All micro-interactions use 300ms timing (no 200ms or 150ms inconsistencies)
</verification>

<success_criteria>
1. All buttons have hover state with color shift and subtle transform (-translate-y-0.5)
2. All navigation and footer links have hover indication (color shift)
3. All hover states use duration-300 for consistent timing
4. Global reduced motion support preserves color feedback while disabling transforms
5. Focus indicators remain visible on all interactive elements
</success_criteria>

<output>
After completion, create `.planning/phases/18-core-micro-interactions/18-01-SUMMARY.md` with:
- Button hover state updates (which pages updated)
- Link hover state verification
- Global reduced motion CSS added
- Any inconsistencies found and resolved
- Deviations from plan (if any)
</output>
