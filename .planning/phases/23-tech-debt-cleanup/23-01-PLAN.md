---
phase: 23-tech-debt-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - composables/usePageTransition.ts
autonomous: true

must_haves:
  truths:
    - "usePageTransition composable no longer exists as dead code"
    - "Page transitions still work correctly (150ms cross-fade)"
    - "Reduced motion support still functions via CSS media queries"
  artifacts:
    - path: "composables/usePageTransition.ts"
      provides: "Should be deleted"
      status: "removed"
  key_links:
    - from: "assets/css/main.css"
      to: "Page transitions"
      via: "CSS @media (prefers-reduced-motion)"
      pattern: "prefers-reduced-motion"
---

<objective>
Remove the unused usePageTransition composable (dead code cleanup)

Purpose: The usePageTransition composable was created in Phase 19 but never consumed. CSS hardcodes the 150ms timing directly and reduced motion support is handled by CSS media queries. Removing this dead code reduces bundle size and codebase complexity.

Output: Deleted composable file, verified transitions still work
</objective>

<execution_context>
@/Users/austinlaw/.claude/get-shit-done/workflows/execute-plan.md
@/Users/austinlaw/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1.2-MILESTONE-AUDIT.md

# Source file to remove
@composables/usePageTransition.ts

# Verify no imports exist
@assets/css/main.css (CSS handles transitions directly)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify no imports and remove dead code</name>
  <files>composables/usePageTransition.ts</files>
  <action>
1. Search entire codebase for any imports of usePageTransition:
   - grep -r "usePageTransition" --include="*.vue" --include="*.ts" (excluding the composable itself)
   - Should find ZERO imports in components/pages

2. Verify CSS handles transitions directly:
   - Check assets/css/main.css for page transition styles
   - Confirm 150ms timing is hardcoded in CSS (not dependent on composable)
   - Confirm @media (prefers-reduced-motion: reduce) block exists

3. Delete the composable:
   - rm composables/usePageTransition.ts

4. Verify build still works:
   - npm run build (should complete without errors)

5. Verify dev server starts:
   - npm run dev (should start without "missing import" errors)
  </action>
  <verify>
- `grep -r "usePageTransition" --include="*.vue" --include="*.ts" .` returns only references in .planning docs
- `npm run build` completes successfully
- `npm run dev` starts without errors
- Page transitions still work (manual test in browser)
  </verify>
  <done>
- usePageTransition.ts file deleted
- Build passes with no import errors
- Page transitions continue to work (CSS-based)
- Codebase has one less unused file
  </done>
</task>

</tasks>

<verification>
1. Grep confirms no runtime imports of usePageTransition exist
2. Build completes successfully (no missing module errors)
3. Dev server starts without warnings about missing composable
4. Navigate between pages to confirm 150ms cross-fade transitions still work
5. Enable OS reduced motion preference and verify transforms are disabled
</verification>

<success_criteria>
- usePageTransition.ts file no longer exists in composables/
- `npm run build` succeeds
- Page transitions function identically to before (CSS-based, 150ms)
- Reduced motion still works via CSS media query
</success_criteria>

<output>
After completion, create `.planning/phases/23-tech-debt-cleanup/23-01-SUMMARY.md`
</output>
