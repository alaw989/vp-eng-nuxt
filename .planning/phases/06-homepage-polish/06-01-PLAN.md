---
phase: 06-homepage-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/HeroSlider.vue
  - pages/index.vue
autonomous: true

must_haves:
  truths:
    - "Hero section displays full-width background image covering entire viewport width"
    - "Dark gradient overlay provides sufficient contrast for text readability"
    - "Single prominent CTA button links to relevant section (View Projects or Our Services)"
    - "Subtle motion effect (parallax or gentle zoom) is smooth and respects prefers-reduced-motion"
    - "Hero images are loaded from optimized /images/hero/ directory with eager loading"
  artifacts:
    - path: "components/HeroSlider.vue"
      provides: "Hero section with parallax motion and dark overlay"
      min_lines: 150
    - path: "pages/index.vue"
      provides: "Homepage using HeroSlider component"
      contains: "HeroSlider"
  key_links:
    - from: "components/HeroSlider.vue"
      to: "/images/hero/*.webp"
      via: "NuxtImg src attribute"
      pattern: "src=.*images/hero"
    - from: "components/HeroSlider.vue"
      to: "VueUse useWindowScroll"
      via: "import from @vueuse/core"
      pattern: "useWindowScroll"
---

<objective>
Implement hero section with full-width background images, dark overlay for text readability, and subtle parallax motion effect.

Purpose: The hero section is the first visual impression of the site and needs to be professional, modern, and engaging. Real hero images from Phase 3 migration should replace placeholder images, with smooth motion effects added using VueUse.

Output: Enhanced HeroSlider component with parallax motion, optimized image loading, and proper dark overlay.
</objective>

<execution_context>
@/home/deck/.claude/get-shit-done/workflows/execute-plan.md
@/home/deck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-homepage-polish/06-CONTEXT.md
@.planning/phases/06-homepage-polish/06-RESEARCH.md
@public/images/image-mapping.json
@components/HeroSlider.vue
@composables/useScrollReveal.ts
</context>

<tasks>

<task type="auto">
  <name>Update HeroSlider with real hero images and parallax motion</name>
  <files>components/HeroSlider.vue</files>
  <action>
    Modify HeroSlider.vue to:

    1. Update slide images to use real hero images from /images/hero/:
       - Slide 1: /images/hero/home-header-1920w.webp (home_header.jpg from live site)
       - Slide 2: /images/hero/skyskr-1-1920w.webp (skyscraper image)
       - Slide 3: /images/hero/uploads-2018-06-1920w.webp (hero image)

    2. Remove secondaryLink/secondaryText to implement single prominent CTA button per context decision:
       - Delete the secondaryLink button and its wrapping condition (lines 66-72)
       - Keep only primaryLink button (lines 59-65)
       - This implements the user's decision for "Single prominent CTA button (e.g., 'View Projects')"

    3. Add parallax motion using VueUse useWindowScroll:
       - Import { useWindowScroll } from '@vueuse/core'
       - Create computed parallaxOffset based on scrollY value (use 0.3 multiplier)
       - Apply transform with translateY to background image container
       - Add will-change: transform for GPU acceleration

    4. Add subtle zoom effect:
       - Create computed zoomScale (1 to 1.15 based on scroll position)
       - Apply transform scale to background image

    5. Ensure proper image attributes for LCP:
       - Keep loading="eager", fetchpriority="high"
       - Keep width="1920" height="1080"
       - Keep format="webp" with placeholder

    6. Respect prefers-reduced-motion:
       - Add @media (prefers-reduced-motion: reduce) query in scoped styles
       - Disable parallax/zoom when user prefers reduced motion

    Keep existing: slide transitions, navigation arrows, dot indicators, progress bar, keyboard/touch support, autoplay pause on hover.
  </action>
  <verify>npm run dev then navigate to http://localhost:3000 and verify hero section displays with images, only ONE CTA button is shown per slide, parallax works on scroll, reduced motion is respected in browser settings</verify>
  <done>Hero section shows full-width background images from /images/hero/, dark overlay ensures text readability, parallax motion works smoothly on scroll (or is disabled with prefers-reduced-motion), single CTA button is prominent</done>
</task>

<task type="auto">
  <name>Verify hero section accessibility compliance</name>
  <files>components/HeroSlider.vue</files>
  <action>
    Verify accessibility-focused items only (functional items already verified in Task 1):

    1. Alt text is descriptive for each slide image
    2. Dark overlay opacity provides WCAG AA contrast for white text
    3. Single CTA button has descriptive text and proper hover states
    4. Keyboard navigation (arrow keys, Home, End) works
    5. Touch swipe works on mobile
    6. Autoplay pauses on hover
    7. ARIA labels are present on all interactive elements

    Check console for any hydration errors or image loading errors.
  </action>
  <verify>Test keyboard navigation, touch gestures, hover pause; check browser console for errors; use Lighthouse accessibility audit</verify>
  <done>No console errors, all keyboard/touch interactions work, ARIA labels present, Lighthouse accessibility score 90+ for hero section</done>
</task>

</tasks>

<verification>
1. Build completes without errors: npm run build
2. No hydration mismatch warnings in browser console
3. Parallax motion is smooth (60fps) on scroll
4. Images load from /images/hero/ directory (check Network tab)
5. prefers-reduced-motion disables motion effects
6. All 3 hero slides cycle through correctly
</verification>

<success_criteria>
- Hero section displays with real images (not placeholders)
- Dark overlay provides sufficient text contrast
- Parallax motion works smoothly on scroll
- Single prominent CTA button per slide
- Motion respects prefers-reduced-motion setting
- Build passes without hydration errors
</success_criteria>

<output>
After completion, create `.planning/phases/06-homepage-polish/06-01-SUMMARY.md`
</output>
